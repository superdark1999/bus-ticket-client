import React from "react";
import { Typography } from "antd";

const { Title } = Typography;
interface SeflProp {}
const Coach = ({}: SeflProp) => {
  return <Title level={2}>Coach</Title>;
};
export default Coach;
